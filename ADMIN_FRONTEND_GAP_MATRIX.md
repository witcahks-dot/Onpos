# PAYPOS / ONPOS2 — Admin, API ve Frontend Uyuşmazlık & Gap Matrisi (Aşama 1)

Bu doküman, projenin gerçek kod tabanında satır satır tespit edilen mimari, alan, yönlendirme, güvenlik ve önbellek uyumsuzluklarını ve önerilen çözümleri içerir.

---

## 1. Tespit Edilen Uyuşmazlıklar ve Çözüm Tablosu

| # | Uyuşmazlık Kategorisi | Gerçek Dosya Yolu & Satır Aralığı | Fonksiyon / Bileşen | Mevcut Durum / Problem | Önerilen Mimari Çözüm |
| :-: | :--- | :--- | :--- | :--- | :--- |
| **1** | **Admin’de var, Frontend’de kullanılmıyor** | `src/types/index.ts:24-28`<br>`src/app/admin/settings/page.tsx:21-528` | `SiteSettings` (`currency`, `language`, `showLogoText`) | `currency` ('₺') ve `language` ('TR') alanları veritabanında ve tip sözleşmesinde tanımlı ancak frontend bileşenlerinde doğrudan sabit `"₺"` ve `"TR"` kullanılmakta. `showLogoText` admin'de var ancak `Header.tsx:62-79`'da logo görseli varsa metin gösterilmemektedir. | Ortak normalizer/DTO katmanında `currency` ve `language` dinamik okunmalı; `showLogoText` ayarı Header bileşenine bağlanmalıdır. |
| **2** | **Frontend’de var, Admin’den düzenlenemiyor** | `src/app/kurumsal/tarihcemiz/page.tsx:12-48`<br>`src/app/kurumsal/belgelerimiz/page.tsx:10-55`<br>`src/app/kurumsal/kariyer/page.tsx:12-65` | `TarihcemizPage`, `BelgelerimizPage`, `KariyerPage` | Tarihçe kilometre taşları (milestones), ISO/TSE sertifika belgeleri ve kariyer açık pozisyonları sayfa dosyası içinde statik TypeScript dizisi (hardcoded) olarak tanımlanmış. Admin'de bunlar için özel CRUD ekranı yoktur. | `CustomPages` veya `AboutPageData` modeli genişletilmeli ya da bu sayfalar `/sayfa/[slug]` dinamik blok yapısına entegre edilmelidir. |
| **3** | **Aynı alanın farklı adlarla kullanılması (Alias & Naming)** | `src/types/index.ts:98` (`isActive`)<br>`src/types/index.ts:42` (`enabled`)<br>`src/types/index.ts:51` (`isVisible`)<br>`src/types/index.ts:343` (`isPublished`)<br>`supabase-schema.sql:57` (`is_active`) | `HeroSlide`, `HomeSectionConfig`, `MenuItem`, `CustomPage` | Durum alanları farklı adlandırılmış: Slaytlar için `isActive`, Menü için `isVisible`, Sayfalar için `isPublished`, Ana sayfa modülleri için `enabled`. SQL şemasında ise `snake_case` (`is_active`, `is_visible`). | Ortak DTO normalizer ile camelCase standartlaştırılmalı, her model için mevcut adlar geriye dönük bozulmadan korunmalıdır. |
| **4** | **Sıralama alanı (Order) İsim Farklılıkları** | `src/types/index.ts:41` (`order`)<br>`src/types/index.ts:172` (`order`)<br>`supabase-schema.sql:38` (`item_order`)<br>`supabase-schema.sql:81` (`product_order`)<br>`supabase-schema.sql:96` (`service_order`) | `MenuItem`, `PosProduct`, `ServiceItem` | TypeScript modellerinde `order: number` kullanılırken, Supabase SQL tablolarında `item_order`, `product_order`, `service_order` kolon adları kullanılmıştır. | Supabase adaptöründe `order <-> *_order` çift yönlü mapping fonksiyonu uygulanmalıdır. |
| **5** | **Slug / Dinamik Route Çakışması ve Çift Yönlendirme** | `src/app/sayfa/[slug]/page.tsx:1-140`<br>`src/app/kurumsal/[slug]/page.tsx:1-136` | `CustomPageDetail` vs `KurumsalCustomPageDetail` | Hem `/sayfa/[slug]` hem de `/kurumsal/[slug]` route'ları `customPages` koleksiyonunu sorgulamaktadır. Admin sayfa listesinde bağlantılar `/sayfa/{slug}` olarak üretilmekte ancak menüde bazı sayfalar `/kurumsal/{slug}` olarak çağrılmaktadır. | Tek bir ortak sayfa çözümleyici helper (`getPageBySlug(slug)`) kullanılmalı; her iki route da aynı normalizer'dan beslenmelidir. |
| **6** | **Referans Sayfası Çift Route'u** | `src/app/referanslar/page.tsx`<br>`src/app/kurumsal/referanslar/page.tsx` | `ReferencesPage` | Referanslar hem `/referanslar` hem de `/kurumsal/referanslar` altında iki ayrı bağımsız sayfa dosyası olarak bulunmaktadır. İkisi de `useCMSStore().references` kullanmaktadır. | Biri diğerine `redirect` yapabilir veya her ikisi ortak `ReferencesView` bileşenini çağırabilir. |
| **7** | **Görsel URL / Bozuk Medya Fallback Yönetimi** | `src/components/layout/Header.tsx:20,66`<br>`src/components/hero/SpatialPosSlider.tsx:40-90`<br>`src/components/home/FeaturedProductsSection.tsx` | `Header`, `SpatialPosSlider`, `FeaturedProductsSection` | `Header.tsx` içinde `imgError` state'i ile görsel yüklenemezse metin logo fallback'i mevcuttur. Ancak `SpatialPosSlider.tsx` ve ürün listelerinde bozuk URL geldiğinde `onError` fallback'i eksiktir; doğrudan kırık görsel ikonu çıkar. | Tüm görsel çağıran bileşenlere merkezi `ImageWithFallback` veya `onError` varsayılan görsel atama desteği eklenmelidir. |
| **8** | **Sunucu Tarafı Yetkilendirme Koruması Eksikliği** | `src/app/api/cms/[entity]/route.ts:35-197`<br>`src/components/admin/AdminGuard.tsx:1-45` | `CMS Route Handler` vs `AdminGuard` | Admin sayfaları istemci tarafında `AdminGuard` ile korunurken, `POST`, `PUT` ve `DELETE` `/api/cms/[entity]` API handler'ları üzerinde sunucu taraflı session/token yetki doğrulaması bulunmamaktadır. | API handler seviyesinde yetki kontrolü yapılmalı; genel ziyaretçilere yalnızca `quote-submit`, `subscribers` ve `GET` istekleri açık bırakılmalıdır. |
| **9** | **Teklif ve İletişim Formunun Admin Entegrasyonu** | `src/components/ui/QuoteModal.tsx:32`<br>`src/components/home/QuoteFormSection.tsx:55`<br>`src/app/admin/submissions/page.tsx` | `QuoteModal`, `QuoteFormSection`, `AdminSubmissionsPage` | ✅ **UYUMLU:** Teklif formu `POST /api/cms/quote-submit` endpoint'ine Zod doğrulamalı olarak yazmakta, kayıt anında `data/cms-db.json` içindeki `submissions` listesine eklenmekte ve `/admin/submissions` ekranında listelenmektedir. | Mevcut başarılı entegrasyon korunacaktır. |
| **10** | **Admin Kaydı Sonrası Önbellek Yenilenmesi (Cache Revalidation)** | `src/lib/cms-store.ts:49-111`<br>`src/lib/cms-db.ts:83-85`<br>`src/app/api/cms/[entity]/route.ts:7-8` | `useCMSStore`, `writeCMSData`, `route.ts` | `route.ts` içinde `force-dynamic` ve `revalidate = 0` tanımlıdır. `writeCMSData` anında `inMemoryCache`'i güncellemektedir. Admin panelinden bir ürün/ayar güncellendiğinde store local state'i ve sunucu belleği aynı anda güncellenir. | Yapı son derece reaktiftir. İkinci tema eklendiğinde aynı mimari korunarak tema ayarı değiştirildiğinde tüm sayfalara yansıması sağlanacaktır. |
| **11** | **Header & Footer Konfigürasyonu Uyuşmazlığı** | `src/types/index.ts:395-454`<br>`src/components/layout/Header.tsx:45-56`<br>`src/components/layout/Footer.tsx:11-65` | `HeaderConfig`, `FooterConfig` | `SiteSettings` içinde `headerStyle` ve `footerStyle` alanları bulunurken, ayrıca `headerConfig` ve `footerConfig` adlı özel nesneler mevcuttur. Admin bazen `settings`, bazen `headerConfig` güncellemektedir. | Ortak DTO katmanında `headerConfig` ve `footerConfig` birleştirilmeli, çakışan alanlar (`headerStyle`, `footerStyle`, `topbarText`) tekilleştirilmelidir. |
| **12** | **Kullanıcı Yetki Rolleri İstemci Denetimi** | `src/app/admin/users/page.tsx`<br>`src/lib/auth-store.ts:5-14` | `useAuthStore` | Admin paneli 'Super Admin', 'Yönetici', 'Editör' rollerini kaydetmektedir ancak sidebar menü linklerinde role-based gizleme (RBAC) yapılmamaktadır; tüm roller tüm admin sekmelerini görmektedir. | Rol bazlı görünürlük kuralları eklenmeli; örneğin Editör rolü Kullanıcı Yönetimi veya Genel Ayarları değiştirememelidir. |

---

## 2. Kontrol Özeti

- **Bulunamayan / Sorunsuz Öğeler:**
  - Teklif formu iletimi ve admin gösterimi: **Tam uyumlu.**
  - E-Bülten aboneliği ve admin gösterimi: **Tam uyumlu.**
  - 3D Spatial Hero Slider ile admin slayt CRUD entegrasyonu: **Tam uyumlu.**
  - Ana sayfa modül açma/kapama ve sıralama mekanizması: **Tam uyumlu.**
